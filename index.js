// Code your solution in this file
function findMatching(drivers, string) {
  for (const driver of drivers) {
  drivers.filter(function (string) { return string.toLowerCase() === drivers.toLowerCase(); });
  }
}